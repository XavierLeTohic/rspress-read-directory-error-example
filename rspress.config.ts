import { defineConfig } from 'rspress/config';

export default defineConfig({
    root: 'docs',
    title: 'Rspress reading directory',
    description: 'Example of rspress trying to read directory error',
});